# 尚食居直播助手 · 发版指南

这份文档说明「怎么发布一个新版本，让用户能收到更新提示并升级」。每次要发新版时，照着下面的步骤走一遍即可。

---

## 一、更新是怎么工作的

应用在两处会用到更新相关的网络数据，**两处必须同步更新，缺一不可**：

| 轨道 | 触发时机 | 数据来源 | 作用 |
|---|---|---|---|
| 轨道一 · 版本提示 | 每次启动应用时静默检查 | jsDelivr CDN 读取仓库的 `package.json` 和 `CHANGELOG.md` | 判断有没有新版本，弹窗显示更新说明 |
| 轨道二 · 实际下载 | 用户点「检查更新」 | GitHub Release 里的 `latest.yml` 和安装包 | 真正下载并覆盖安装 |

**关键**：轨道一读的是「版本号」，轨道二读的是「安装包」。如果只改了版本号但没发 Release，用户会看到「有新版本」却下载失败。所以发版时两处都要做。

相关代码位置：`electron/main/managers/UpdateManager.ts`（第 33-35 行是仓库配置）。

---

## 二、发版五步

### 第 1 步：改版本号

打开 `package.json`，把 `version` 改成新版本号。

```json
"version": "1.6.2"
```

版本号规则：`主版本.次版本.修订号`。修 bug 改第三位，加功能改第二位，大改版改第一位。

### 第 2 步：写更新说明

打开 `CHANGELOG.md`，在 `## v1.6.2` 这样的段落位置（`# Changelog` 标题下方、其他版本段落的上方）插入新版本内容：

```markdown
## v1.6.2

### 更新内容

- 修复了某某问题
- 新增了某某功能
```

注意：版本号必须带 `v` 前缀，且要和上一步 `package.json` 里写的完全一致。这段内容就是用户在更新弹窗里看到的文字。

### 第 3 步：打包

在项目目录打开终端，执行：

```bash
pnpm build
```

打包完成后，产物在项目根目录的 `release/1.6.2/` 文件夹里（数字是版本号）。里面会有这些文件：

| 文件 | 是否必须上传 | 说明 |
|---|---|---|
| `oba-live-tool_1.6.2_windows_x64.exe` | 必须 | 安装包本体 |
| `latest.yml` | **必须** | 更新元数据，少了它应用检测不到新版本 |
| `oba-live-tool_1.6.2_windows_x64.exe.blockmap` | 建议 | 增量更新用，能让老用户只下载差异部分 |

### 第 4 步：提交并推送代码

```bash
git add .
git commit -m "chore: 发布 v1.6.2"
git push origin main
```

这一步很重要：轨道一是从 GitHub 仓库的 `main` 分支读版本号和更新说明的，**不推送的话用户端永远看不到新版本**。

### 第 5 步：发布 GitHub Release

1. 打开 https://github.com/xinyang2009-droid/oba-live-tool/releases/new
2. **Tag version** 填 `v1.6.2`（带 v，和 CHANGELOG 里的保持一致），然后点击「Create new tag」
3. **Release title** 填 `v1.6.2`
4. **描述** 可以填和第 2 步一样的更新内容（可选，弹窗里显示的是 CHANGELOG，不是这里）
5. 把第 3 步 `release/1.6.2/` 里的**三个文件全部拖进去上传**
6. 确认仓库是 **Public**（私有仓库下载需要额外配置 Token，会失败）
7. 点击 **Publish release**

---

## 三、发版后怎么验证

1. 等 5～30 分钟（jsDelivr CDN 需要时间同步，首次可能更久）
2. 用老版本的应用启动，应该会弹出更新提示
3. 点「检查更新」应该能正常下载安装

如果启动后没提示，先检查这个地址能不能打开：

```
https://fastly.jsdelivr.net/gh/xinyang2009-droid/oba-live-tool@main/package.json
```

打不开或显示的还是旧版本号，就是 CDN 还没同步，等一会儿再试。

---

## 四、常见问题

**Q：用户点了检查更新但下载失败？**
A：多半是 GitHub Release 里漏传了 `latest.yml`，或者 Release 被设成了私有。

**Q：提示「有新版本」但点下载报错？**
A：版本号改了但 Release 还没发，或者 Release 的 tag 名和版本号对不上（比如版本号是 1.6.2，tag 写成了 1.6.2 少了 v）。

**Q：更新说明显示的是原作者的内容？**
A：`CHANGELOG.md` 里还残留着原项目的历史段落。程序读到「版本号小于等于用户当前版本」就停止，所以只要你的版本段落写在最上面，就不会显示旧内容。

**Q：想临时关掉更新功能？**
A：打开 `electron/main/managers/UpdateManager.ts`，把第 31 行的 `UPDATER_DISABLED` 改成 `true`。改回来就设成 `false`。

**Q：GitHub 下载太慢怎么办？**
A：应用内置了代理更新源的机制。如果后期需要，可以在 `UpdateManager.ts` 的 `GITHUB_OWNER` 附近加一个国内加速前缀（如 `https://ghfast.top/`），具体做法另说。

---

## 五、快速检查清单

发版前对着过一遍：

- [ ] `package.json` 的 version 已改
- [ ] `CHANGELOG.md` 顶部加了新版本段落，版本号带 `v` 且与 package.json 一致
- [ ] `pnpm build` 执行成功，`release/新版本号/` 里有 exe、latest.yml、blockmap 三个文件
- [ ] 代码已 commit 并 push 到 GitHub
- [ ] GitHub Release 已发布，tag 带 `v`，三个文件都已上传
- [ ] Release 是 Public 状态
