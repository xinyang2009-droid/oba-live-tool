# Changelog

本文件记录「尚食居直播助手」的版本更新内容。

## 维护规则（重要）

每次发版时，在文件顶部追加一个新段落，格式必须严格如下：

```
## vX.Y.Z

### 更新内容

- 这里写这次改了什么
- 一行一条，用减号开头
```

注意事项：

1. `## vX.Y.Z` 中的版本号必须带 `v` 前缀，且要和 `package.json` 里的 `version` 完全一致。
2. 新版本段落要放在**最上面**（排在 `# Changelog` 标题下面），程序是从上往下读的。
3. 程序读到「版本号小于等于用户当前版本」的段落就会停止，所以顺序错了会显示重复内容。
4. 支持 Markdown 语法，弹窗里会自动渲染成好看的排版。

## v1.6.2

### 更新内容

- 窗口标题改为「尚食居直播助手」
- 移除设置页中的项目信息入口
- 更新源切换至团队自有仓库
